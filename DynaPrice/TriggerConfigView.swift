import SwiftUI

struct TriggerConfigView: View {
    var body: some View {
        NavigationView {
            Text("Configure price change triggers")
                .navigationTitle("Triggers")
        }
    }
}

#Preview {
    TriggerConfigView()
}
