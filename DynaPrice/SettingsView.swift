import SwiftUI

struct SettingsView: View {
    var body: some View {
        NavigationView {
            Text("App settings")
                .navigationTitle("Settings")
        }
    }
}

#Preview {
    SettingsView()
}
